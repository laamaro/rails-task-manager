<h2><%= @task.title %></h2>
<h3><%= @task.details %></h3>
<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Want to delete this task?" } %>
<%= link_to "Back to list", tasks_path %>
<%= link_to "Edit task", edit_task_path %>
